import MainLayout from "@/components/layouts/mainLayout/MainLayout";
import { FC } from "react";

const Contacts: FC = () => {

    return (
        <MainLayout title="Contacts">Contacts</MainLayout>
    )
}

export default Contacts;